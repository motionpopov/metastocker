name: metastocker
services:
  web:
    image: caddy@sha256:ec18ee54aab3315c22e25f3b2babda73ff8007d39b13b3bd1bfffa2f0444c7d9
    container_name: metastocker-web
    restart: unless-stopped
    user: "1000:1000"
    command: ["caddy", "run", "--config", "/srv/metastocker/current/Staticfile", "--adapter", "caddyfile"]
    volumes:
      - /opt/metastocker:/srv/metastocker:ro
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid
      - /data:size=1m,noexec,nosuid
      - /config:size=1m,noexec,nosuid
    cap_drop: [ALL]
    # The upstream Caddy binary carries this file capability, even on port 8080.
    cap_add: [NET_BIND_SERVICE]
    security_opt: ["no-new-privileges:true"]
    mem_limit: 128m
    cpus: 0.5
    pids_limit: 100
    environment:
      GOMEMLIMIT: 96MiB
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/release.json"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    networks: [proxy, stats]
  analytics:
    image: metastocker-analytics:@RELEASE@
    build:
      context: /opt/metastocker/releases/@RELEASE@/server
    container_name: metastocker-analytics
    restart: unless-stopped
    user: "1001:1001"
    volumes:
      - /opt/metastocker/data:/data
      - /opt/metastocker/secrets/auth.json:/run/secrets/auth.json:ro
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid
    cap_drop: [ALL]
    security_opt: ["no-new-privileges:true"]
    mem_limit: 256m
    cpus: 0.5
    pids_limit: 64
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8081/health').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: 5m
        max-file: "2"
    networks: [stats]
networks:
  stats:
    internal: true
  proxy:
    external: true
    name: vintage-inventory_default
